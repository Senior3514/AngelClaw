# AngelClaw environment configuration
# Copy to angelgrid.env and edit as needed.
# This file is read by docker-compose and the systemd service.
#
# Note: The product name is AngelClaw. ANGELCLAW_ env var prefixes are
# preferred but ANGELGRID_ prefixes are also supported for compatibility.

# Tenant ID for multi-tenant Cloud
ANGELGRID_TENANT_ID=default

# Cloud URL (for remote ANGELNODE agents connecting to a central Cloud)
# Leave as-is for single-VPS deployment.
ANGELCLAW_CLOUD_URL=http://cloud:8500
ANGELGRID_CLOUD_URL=http://cloud:8500

# Policy sync interval (seconds)
ANGELGRID_SYNC_INTERVAL=60

# LLM Proxy (set to true + pull a model to enable)
LLM_ENABLED=false
LLM_MODEL=llama3

# ANGELNODE status endpoint token (optional, recommended for production)
# ANGELNODE_STATUS_TOKEN=your-secret-token-here

# --- Auth (V0.4.0+) ---
# ANGELCLAW_AUTH_ENABLED=true
# ANGELCLAW_AUTH_MODE=local
# ANGELCLAW_ADMIN_USER=admin
# ANGELCLAW_ADMIN_PASSWORD=changeme
# ANGELCLAW_VIEWER_USER=viewer
# ANGELCLAW_VIEWER_PASSWORD=viewerpass
# ANGELCLAW_JWT_SECRET=auto-generated-if-not-set

# --- Binding ---
# ANGELCLAW_BIND_HOST=127.0.0.1
# ANGELCLAW_BIND_PORT=8500

# --- Webhooks ---
# ANGELCLAW_WEBHOOK_URL=https://your-siem.example.com/webhook
# ANGELCLAW_WEBHOOK_SECRET=your-hmac-secret
